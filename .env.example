# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI Configuration (LOCAL TESTING ONLY)
# For production, set OPENAI_API_KEY in Supabase Edge Function secrets:
# supabase secrets set OPENAI_API_KEY=sk-your-key-here
VITE_OPENAI_API_KEY=sk-your-openai-key-here

# Setup Instructions:
# 1. Copy this file:
#    cp .env.example .env.local
#
# 2. Fill in your actual values in .env.local
#
# 3. NEVER commit .env.local to Git (it's in .gitignore)
#
# 4. For production deployment:
#    - Supabase keys: Set in hosting platform environment variables
#    - OpenAI key: Set in Supabase secrets (server-side only)

# Security Notes:
# - .env.local is for LOCAL DEVELOPMENT only
# - VITE_ prefix makes variables accessible in frontend (public)
# - OpenAI key should NOT have VITE_ prefix in production
# - Store OpenAI key in Supabase Edge Function secrets for production
